# FlightRadar24 official Python SDK
fr24>=0.3.0

# HTTP client (dependency of fr24, but listed explicitly)
httpx[http2]
h2>=4.0

# Environment variable management
python-dotenv>=1.0.0

# Web interface
flask
folium

# Protobuf (dependency of fr24)
protobuf
grpcio

# Maps, images, HTTP utilities
Pillow
requests

# ISS pass prediction (optional feature — ISS_ENABLED; module degrades
# gracefully if missing)
ephem

# ATC radio (optional feature — ATC_ENABLED; utilities/atc_audio.py imports
# these lazily and degrades gracefully if missing). Install only what your
# selected output needs:
#   usb output:        sudo apt install mpv   (plus ffmpeg for the relay)
#   chromecast output: pip install pychromecast zeroconf
#   airplay output:    pip install pyatv
# pychromecast
# zeroconf
# pyatv
